*.js
!jest.config.js
*.d.ts
# Vite 앱의 src/ 안 타입 선언 (vite-env.d.ts) 만 트래킹.
# web/vite.config.d.ts 같은 `tsc -b` 산출물은 제외.
!web/src/**/*.d.ts
node_modules
*.tsbuildinfo

# CDK asset staging
.cdk.staging
cdk.out

# Vite 빌드 출력 — BucketDeployment 가 직접 읽으므로 git 에는 안 들어감
web/dist

# 환경변수 / 시크릿
.env
.env.*
!.env.sample
*.pem
*.key
*-credentials.json

# 로컬 테스트 아티팩트 (curl payload / 출력) — Day 7 패턴과 동일
payload.json
payload*.json
check*.json
out.json
